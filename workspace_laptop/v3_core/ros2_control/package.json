{
  "name": "@robot-mesero/ros2-control",
  "version": "3.0.0",
  "description": "Puente ROS2 — Comandos, telemetría, navegación",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "start": "echo 'Módulo de librería — usar desde backend_api'",
    "test": "node --test tests/**/*.test.mjs",
    "test:watch": "node --test --watch tests/**/*.test.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}
